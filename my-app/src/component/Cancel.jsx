import { Link } from "@tanstack/react-router";
const Cancel = () => {
  return (
    <div>
      <h1>Payment Cancel</h1>
      <Link to="/home_page">Back to home</Link>
    </div>
  );
};

export default Cancel;
Cancel;
